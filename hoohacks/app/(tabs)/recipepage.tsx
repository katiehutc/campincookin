// generatepage.tsx (example)
import React from 'react';
import { View, Text } from 'react-native';

const RecipePage = () => {
    return (
        <View>
            <Text>Generate Page</Text>
        </View>
    );
};

export default RecipePage; // Ensure the component is exported as default
