// generatepage.tsx (example)
import React from 'react';
import { View, Text } from 'react-native';

const RecipeList = () => {
    return (
        <View>
            <Text>Generate Page</Text>
        </View>
    );
};

export default RecipeList; // Ensure the component is exported as default
